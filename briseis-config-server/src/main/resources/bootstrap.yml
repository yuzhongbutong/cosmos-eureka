spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yuzhongbutong/cosmos-app-config
          search-paths: config
          username:
          password:
      label: master